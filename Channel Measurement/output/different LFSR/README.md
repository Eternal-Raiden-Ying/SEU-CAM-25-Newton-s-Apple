**Here are the results about using scrambler to different types of files.**  


    The point is that when we get constellations directly from bits, there always exists
    extreme sharp peaks, which subsequently lower the avg power and decrease SNR (signal noise ratio),
    in that case, the channel capacity drops dramatically, and you could not get expected result.
    
    Something more about the details:
        --different types of LFSR are designed based on the table of primitive polynomial.
        --scrambled data in the right pic is all normalized. some seems not because we only show part of
        the signal so that it won't overlap too much, in that case, you can't see anything
        --the data is just the signal that an emitter will send, because we here only care about
        if the avg power and peak power is suitable